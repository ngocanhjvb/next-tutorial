import React from 'react';
import PropTypes from 'prop-types';

const Error = props => {
    return (
        <div>
            Somethings is not right !!!
        </div>
    );
};

Error.propTypes = {

};

export default Error;
